import {supabase} from '../client'
import {useParams} from 'react-router-dom'

const Details = () => {

    const {id} = useParams();
    return (
        <div>
            
        </div>
    );
}

export default Details;